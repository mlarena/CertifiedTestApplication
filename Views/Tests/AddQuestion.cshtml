@model CertifiedTestApplication.Models.Entities.Question

<h2>Добавить вопрос</h2>

<div class="row">
    <div class="col-md-6">
        <form asp-action="AddQuestion" enctype="multipart/form-data">
            <input type="hidden" asp-for="TestId" />
            
            <div class="mb-2">
                <label class="form-label small">Текст вопроса</label>
                <textarea asp-for="Text" class="form-control form-control-sm" rows="3" required></textarea>
            </div>

            <div class="mb-2">
                <label class="form-label small">Тип вопроса</label>
                <select asp-for="Type" class="form-select form-select-sm" 
                        asp-items="Html.GetEnumSelectList<CertifiedTestApplication.Models.Entities.QuestionType>()"></select>
            </div>

            <div class="mb-2">
                <label class="form-label small">Порядок (сортировка)</label>
                <input asp-for="Order" type="number" class="form-control form-control-sm" />
            </div>

            <div class="mb-3">
                <label class="form-label small">Изображение (необязательно)</label>
                <input type="file" name="image" class="form-control form-control-sm" accept="image/*" />
            </div>

            <div class="mt-3">
                <button type="submit" class="btn btn-primary btn-sm">Далее (добавить ответы)</button>
                <a asp-action="Edit" asp-route-id="@Model.TestId" class="btn btn-link btn-sm text-secondary">Отмена</a>
            </div>
        </form>
    </div>
</div>
